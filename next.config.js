/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/cocktails',
  reactStrictMode: true,
  // TODO: enable
  // typedRoutes: true,
};

export default nextConfig;
